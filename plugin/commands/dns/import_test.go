package dns_test

import (
	. "github.com/onsi/ginkgo"
)

var _ = Describe("DNS Import", func() {
	/*
		var (
			fakeUI         *terminal.FakeUI
			fakeDNSManager *testhelpers.FakeDNSManager
			cmd            *dns.ImportCommand
			cliCommand     cli.Command
		)
		BeforeEach(func() {
			fakeUI = terminal.NewFakeUI()
			fakeDNSManager = new(testhelpers.FakeDNSManager)
			cmd = dns.NewImportCommand(fakeUI, fakeDNSManager)
			cliCommand = cli.Command{
				Name:        metadata.CMD_DNS_IMPORT.Name,
				Description: metadata.CMD_DNS_IMPORT.Description,
				Usage:       metadata.CMD_DNS_IMPORT.Usage,
				Flags:       metadata.CMD_DNS_IMPORT.Flags,
				Action:      cmd.Run,
			}
		})

		Describe("DNS import", func() {
			// Context("DNS import with non-exist file", func() {
			// 	It("return error", func() {
			// 		err := testhelpers.RunCommand(cliCommand, "abc")
			// 		Expect(err).To(HaveOccurred())
			// 		Expect(strings.Contains(err.Error(), "Invalid input for 'Volume ID'. It must be a positive integer.")).To(BeTrue())
			// 	})
			// })
		})
	*/
})
